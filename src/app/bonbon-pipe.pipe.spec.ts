import { BonbonPipePipe } from './bonbon-pipe.pipe';

describe('BonbonPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BonbonPipePipe();
    expect(pipe).toBeTruthy();
  });
});
